import { ArrowLeft, ArrowRight, BookOpen, CheckCircle2, Eye, EyeOff, LockKeyhole, Mail, UserRound } from 'lucide-react';
import { useState } from 'react';

const initialForm = { name: '', studentId: '', email: '', department: 'Computer Science and Engineering', year: 'IV', password: '', confirmPassword: '' };

export default function Register({ onSubmit, onLogin, onBack }) {
    const [form, setForm] = useState(initialForm);
    const [showPassword, setShowPassword] = useState(false);
    const [error, setError] = useState('');
    const update = (field) => (event) => setForm((current) => ({ ...current, [field]: event.target.value }));
    const submit = (event) => { event.preventDefault(); const message = onSubmit(form); if (message) setError(message); };
    return <main className="auth-page form-page"><section className="auth-visual compact-visual register-visual"><div className="auth-brand"><span className="auth-brand-mark"><BookOpen size={21} /></span><strong>Digital Library</strong></div><div className="login-quote"><p className="eyebrow light-eyebrow">Join the collection</p><h2>Make room for<br /><em>new ideas.</em></h2><div className="quote-rule" /><p>Create your student account and make every checkout count.</p></div><div className="visual-bookmark"><CheckCircle2 size={18} /><span>YOUR READING SPACE</span></div></section><section className="auth-form-panel register-panel"><button className="auth-back" onClick={onBack}><ArrowLeft size={16} /> Back to welcome</button><div className="form-heading compact-heading"><div className="form-icon"><UserRound size={20} /></div><p className="eyebrow">New member</p><h1>Create your account</h1><p>Set up your local student library profile.</p></div><form className="auth-form register-form" onSubmit={submit}><div className="field-grid"><label>Full name<div className="field-wrap"><UserRound size={16} /><input value={form.name} onChange={update('name')} placeholder="Tejasri Karthikeyan" /></div></label><label>Student ID<div className="field-wrap"><UserRound size={16} /><input value={form.studentId} onChange={update('studentId')} placeholder="CSE2026XXX" /></div></label></div><label>Email address<div className="field-wrap"><Mail size={17} /><input type="email" value={form.email} onChange={update('email')} placeholder="student@library.com" /></div></label><div className="field-grid"><label>Department<select value={form.department} onChange={update('department')}><option>Computer Science and Engineering</option><option>Information Technology</option><option>Electronics and Communication</option><option>Mechanical Engineering</option></select></label><label>Year<select value={form.year} onChange={update('year')}><option>I</option><option>II</option><option>III</option><option>IV</option></select></label></div><div className="field-grid"><label>Password<div className="field-wrap"><LockKeyhole size={16} /><input type={showPassword ? 'text' : 'password'} value={form.password} onChange={update('password')} placeholder="At least 6 characters" /><button type="button" className="field-action" onClick={() => setShowPassword((visible) => !visible)} aria-label="Toggle password visibility">{showPassword ? <EyeOff size={16} /> : <Eye size={16} />}</button></div></label><label>Confirm password<div className="field-wrap"><LockKeyhole size={16} /><input type={showPassword ? 'text' : 'password'} value={form.confirmPassword} onChange={update('confirmPassword')} placeholder="Repeat password" /></div></label></div>{error && <p className="form-error">{error}</p>}<button className="primary-button auth-submit" type="submit">Create Account <ArrowRight size={17} /></button></form><p className="form-switch">Already have an account? <button onClick={onLogin}>Login</button></p></section></main>;
}
